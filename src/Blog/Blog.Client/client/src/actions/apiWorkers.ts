import axios from "axios";
import { useEffect, useState } from "react";

export function getSingle<T>(url: string): T {
    return {} as T;
}

export function getList<T>(url: string): T[] {
    return [{} as T];
}